[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mesh-models"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/mesh_models"]
