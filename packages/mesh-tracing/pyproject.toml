[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mesh-tracing"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["langfuse>=2.0,<3.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/mesh_tracing"]
